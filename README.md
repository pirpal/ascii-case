ASCII CASE
---------------

Basic C functions to manage basic ASCII letters (A-Z and a-z) case

+ `is_lower(char) -> bool`
+ `is_upper(char) -> bool`
+ `lowercase(char) -> char`
+ `uppercase(char) -> char`
+ `togglecase(char) -> char`
